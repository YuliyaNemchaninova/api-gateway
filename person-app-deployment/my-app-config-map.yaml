apiVersion: v1
kind: ConfigMap
metadata:
  name: my-app-config-map
data:
  # property-like keys; each key maps to a simple value
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql-dev.auth.svc.cluster.local:5432/app_db